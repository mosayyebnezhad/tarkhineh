import { CartAlt } from "iconoir-react"
import { appContext } from "../../App";
import { useContext, useEffect } from "react";
import api from "../../Api/api";

const Cart = () => {



    // useEffect(()=>{
    //     console.log(Cart)
    //     const fetchData = async () => {
    //         await api.get("/food/One",{params:{productID:FoodsID[0]}}).then(res=>{console.log(res.data)})
    //     }
    //     fetchData()

    // },[Cart])

    return (
        <>
            {/* {FoodsID} */}
        </>
    )
}

export default Cart;